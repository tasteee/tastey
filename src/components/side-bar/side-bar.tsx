import { styled } from '#/modules/tw-styled'

const SideBarBox = styled.div('SideBarBox', 'fixed top-16 left-0 bottom-0 w-64 border-r border-border bg-background/80 backdrop-blur-sm')

export const SideBar = () => {
	// should have
}
