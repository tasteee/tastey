# twon

```
npm i twon
```

```js
styled.div('flex gap-2') // displayName: styled.div
styled.h1('Foo', 'flex gap-2') // displayName: Foo
styled(Button, 'rounded-xl') // displayName: styled(Button)
styled(Button, 'PrimaryButton', '...') // displayName: PrimaryButton
```
